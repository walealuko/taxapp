{
  "buildCommand": "cd taxapp && npx expo export",
  "outputDirectory": "taxapp/dist",
  "installCommand": "cd taxapp && npm install",
  "framework": null,
  "files": ["taxapp/**", "!.git/**"],
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
