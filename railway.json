{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacks": {
      "installCommand": "npm install --omit=dev 2>&1 || npm install 2>&1",
      "startCommand": "node server.js"
    }
  },
  "deploy": {
    "startCommand": "node server.js"
  }
}
